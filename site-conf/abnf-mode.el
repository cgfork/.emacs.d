;;; package --- A major mode for editing ABNF grammar -*- lexical-binding: t -*-
;;; Commentary:
;;; Code:

(defvar abnf-mode-font-lock-keywords nil "Font lock ABNF for ABNF mode.")
(setq abnf-mode-font-lock-keywords
      '())

(provide 'abnf-mode)
;;; abnf-mode.el ends here
